# Semantic Search Chatbot with LlamaIndex and OpenAI

This project implements a semantic search chatbot that indexes documents and allows users to query information using natural language. It uses LlamaIndex for document indexing and OpenAI's GPT-3.5-turbo for generating responses.

## Features
- **Document Indexing**: Indexes documents for semantic search using LlamaIndex.
- **Prompt Helper**: Configures input chunking for efficient processing and response generation.
- **Query Engine**: Allows users to query indexed documents and receive contextually relevant responses.

## Setup

1. **Clone the Repository**:
   ```bash
   git clone https://github.com/yourusername/Document-Processing-and-Retrieval.git
   cd Document-Processing-and-Retrieval/Semantic-Search-Chatbot

Install Requirements:
pip install -r requirements.txt

Configure API Key:

Copy config_example.json to config.json.
Add your OpenAI API key to the config.json file.

Usage
Index Documents:

Run the indexing function on your document folder:
python document_search_chatbot.py

Query the Bot:

Once indexed, you can query the bot by calling my_chatGPT_bot() function with a question:
print(my_chatGPT_bot('Tell me the summary of what happened in Borisov, Russia'))

Output:

Query responses are generated by OpenAI’s model based on indexed documents.
Project Files
document_search_chatbot.py: Main script for indexing and querying documents.
requirements.txt: Lists dependencies for the project.
config_example.json: Sample configuration for API key.
Future Enhancements
Add document similarity and clustering features.
Integrate more advanced search functionalities, such as multi-document summarization.

